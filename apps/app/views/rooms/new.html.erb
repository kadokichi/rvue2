<% content_for :title, "New room" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">新規ルーム</h1>

  <%= render "form", room: @room %>

  <%= link_to "Back to rooms", rooms_path, class: "link_btn_ml2" %>
</div>
